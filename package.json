{
  "name": "auto-web-test",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:expense": "playwright test tests/expense-workflow.spec.ts --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "dotenv": "^17.4.2",
    "typescript": "^5.9.0",
    "zod": "^4.4.3"
  }
}
